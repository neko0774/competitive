#include<bits/stdc++.h>
using namespace std;

int solve(int H, int W, int sx, int sy, vector<string>> &S){

}


int main(){
  int H, W;
  cin >> H >> W;
  vector<string> S(H);
  for(int i = 0; i < H; i++){
    cin >> S[i];
  }

  for(int sx = )

  for(int y = 0; y < H; y++){
    for(int x = 0; x < W; x++){
      if(S[y][x] == '#'){

      }
    }
  }
}