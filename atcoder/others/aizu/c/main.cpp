#include<bits/stdc++.h>
using namespace std;

vector<int> parse(string S){
  int depth = 0;
  int cur = 0;
  while(cur<S.size()){
    if(S[cur]=='('){
      depth++;
    }
    if(S[cur]==')'){
      depth--;
    }
    if(S[cur] = '['){
      
    }

  }
}

int main(){

}