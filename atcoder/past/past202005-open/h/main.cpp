#include <bits/stdc++.h>
using namespace std;

int main(){
  int N, L;
  cin >> N >> L;
  vector<int> X(N, 0);
  for(int i=0;i<N;i++){
    cin >> X[i];
  }
  vector<int> T(3, 0);
  for(int i=0;i<3;i++){
    cin >> T[i];
  }

  
}