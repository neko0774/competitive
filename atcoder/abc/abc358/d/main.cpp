#include <bits/stdc++.h>
using namespace std;
#define ll long long


int main(){
  int N, M;
  cin >> N >> M;
  vector<int> A(N), B(M);
  set<int> 
  for(int i=0;i<N;i++){
    cin >> A[i];
  }
  for(int j=0;j<M;j++){
    cin >> B[i];
  }
  sort(B.beign(), B.end());

}