#include <bits/stdc++.h>
using namespace std;
#define ll long long


int main(){
  int N, M, Q;
  cin >> N >> M >> Q;
  vector<vector<int>> query();
  int q, l, r, x;
  unordered_set<int> used;
  for(int i=0;i<Q;i++){
    cin >> q;
    if(q==1){
      cin >> l >> r >> x;
    }else if(q==2){
      cin >> l >> x;
    }else{
      cin >> l >> r;
    }
  }

}