#include<bits/stdc++.h>
using namespace std;
void solve(){
  int N;
  cin >> N;
  vector<int> A(N);
  vector<int> idx;
  for(int i=0;i<N;i++){
    cin >> A[i];
    if(A[i]!=-1)idx.push_back(i);
  }
  for(int i=0;i<N;i++){
    
  }

}

int main(){
  int T;
  cin  >> T;
  while(T--) solve();
}